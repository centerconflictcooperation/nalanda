% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_pdf_text_with_llm.R
\name{extract_pdf_text_with_llm}
\alias{extract_pdf_text_with_llm}
\title{Extract text from a PDF with a multimodal LLM}
\usage{
extract_pdf_text_with_llm(
  pdf_path,
  prompt = paste("Transcribe the main body text from this PDF as plain UTF-8 text.",
    "Keep the wording faithful to the source.",
    "Ignore repeated stamps, watermarks, page numbers, headers, footers,",
    "and other obvious non-book overlays when they are not part of the book.",
    "Preserve paragraph breaks.", "Return only the extracted text.",
    "Do not add any introduction, explanation, summary, XML, markdown fences,",
    "or labels such as 'The following is the main body text from the PDF:'.", sep = " "),
  model = "gpt-5-mini",
  integration = getOption("nalanda.integration"),
  virtual_key = getOption("nalanda.virtual_key"),
  base_url = getOption("nalanda.base_url"),
  temperature = 1,
  seed = 42,
  output_path = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{pdf_path}{Character scalar path to a local PDF file, a character vector
of PDF paths, or a named/nested list of PDF paths such as the output of
\code{list_book_chapters(extension = "pdf")}.}

\item{prompt}{Character scalar instruction shown alongside the PDF. The
default asks for faithful transcription while ignoring obvious non-book
overlays such as repeated stamps, page numbers, and headers/footers.}

\item{model}{Character. Model name for the chat backend.}

\item{integration}{Optional Portkey/gateway route slug. If supplied and
\code{model} is not fully-qualified, nalanda will build
\code{"@{integration}/{model}"}.}

\item{virtual_key}{Optional legacy virtual key. If supplied and \code{model} is
not fully-qualified, nalanda will build \code{"@{virtual_key}/{model}"}.}

\item{base_url}{Character. Base URL for API calls.}

\item{temperature}{Numeric. Sampling temperature passed to the backend.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{output_path}{Optional output target. For a single PDF, this may be
either an exact \code{.txt} file path or a directory path. For a character
vector or nested list of PDFs, supply a directory-like path without a file
extension; nalanda will write one \code{.txt} per PDF incrementally,
preserving partial progress if a later file fails.}

\item{overwrite}{Logical scalar. If \code{TRUE}, replace existing output files at
\code{output_path}. Defaults to \code{FALSE}.}
}
\value{
If \code{pdf_path} is a single file, a character scalar containing the
extracted text. If \code{pdf_path} is a character vector or nested list, returns
text with the same structure and names as the input. If \code{output_path} is
supplied, text files are also written to disk.
}
\description{
This helper uploads a local PDF to a multimodal model through \code{ellmer} and
asks the model to return clean running text. It is useful when ordinary OCR
struggles with stamps, overlays, or poor scan quality but the target model
can read PDFs directly.
}
\details{
In testing through the NYU Portkey/gateway path, PDF extraction was more
reliable with \code{gpt-5-mini} than with Gemini routes. Gemini-family models may
still work in other environments, but PDF handling through
\code{chat_portkey()} was inconsistent in our tests.
}
