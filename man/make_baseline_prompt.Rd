% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ai_on_chapters.R
\name{make_baseline_prompt}
\alias{make_baseline_prompt}
\title{Build the baseline (Turn 1) prompt}
\usage{
make_baseline_prompt(
  identity_context,
  question_template,
  groups,
  identity_label
)
}
\arguments{
\item{identity_context}{Character scalar. The full context string for this
identity.}

\item{question_template}{Character scalar. Optionally contains \code{{group}}
placeholder for per-group expansion.}

\item{groups}{Character vector of all group labels.}

\item{identity_label}{Character scalar. The group label assigned as identity
(used to determine ingroup-first ordering).}
}
\value{
Character scalar prompt.
}
\description{
Constructs the prompt: identity context + question(s). If the question
template contains \code{{group}}, it is expanded once per group (ingroup first).
Otherwise, the question is used as-is (single-question mode).
}
\examples{
# Per-group mode (asks about each group, ingroup first):
make_baseline_prompt(
  identity_context = "You are simulating an American Democrat.",
  question_template = "How warmly (0-100) do you feel towards {group}s?",
  groups = c("Democrat", "Republican"),
  identity_label = "Democrat"
)

# Single-question mode (asks once, as-is):
make_baseline_prompt(
  identity_context = "You are simulating an American Democrat.",
  question_template = "How warmly (0-100) do you feel towards your outgroup?",
  groups = c("Democrat", "Republican"),
  identity_label = "Democrat"
)

}
