% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompt_grid.R
\name{collect_prompt_grid_results}
\alias{collect_prompt_grid_results}
\title{Collect prompt-grid checkpoints into one reusable results table}
\usage{
collect_prompt_grid_results(output_dir, existing_results = NULL)
}
\arguments{
\item{output_dir}{Character vector of checkpoint directories created by
\code{\link[=run_prompt_grid]{run_prompt_grid()}}.}

\item{existing_results}{Optional results data frame, run bundle, RDS path,
or CSV path accepted by the \code{existing_results} argument of
\code{\link[=run_prompt_grid]{run_prompt_grid()}}.}
}
\value{
A tibble of raw prompt-grid results. It can be passed directly to
\code{run_prompt_grid(existing_results = ...)}.
}
\description{
Read every valid prompt-grid checkpoint in one or more directories and
combine it with optional prior results. This is useful after cost-gated
phases because checkpoints for models that are inactive in the current
configuration remain available for later analysis and reuse.
}
\details{
Checkpoint rows are identified by the strong \code{task_hash} stored with each
checkpoint and by \code{input_row}. Exact duplicate rows are removed. Conflicting
rows with the same task and input identities cause an error rather than
being silently resolved.
}
