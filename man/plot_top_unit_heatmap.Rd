% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_top_units.R
\name{plot_top_unit_heatmap}
\alias{plot_top_unit_heatmap}
\title{Plot model-by-unit rank heatmap}
\usage{
plot_top_unit_heatmap(
  data,
  item_col = NULL,
  model_col = "model",
  facet_by = NULL,
  top_n_items = NULL,
  show_values = TRUE,
  title = "Unit ranks by model"
)
}
\arguments{
\item{data}{The \code{ranks} data frame from \code{\link[=summarize_top_units]{summarize_top_units()}} with
\code{include_ranks = TRUE}.}

\item{item_col}{Character. Column identifying the ranked item. If \code{NULL},
the function tries to infer \code{"book"} or \code{"book_id"}.}

\item{model_col}{Character. Column identifying the model (default
\code{"model"}).}

\item{facet_by}{Optional character vector of columns to facet by, e.g.
\code{"party"}.}

\item{top_n_items}{Optional integer. If supplied, keep only the best
\code{top_n_items} per facet, based on average rank across models.}

\item{show_values}{Logical. If \code{TRUE} (default), print rank values in cells.}

\item{title}{Optional plot title.}
}
\value{
A ggplot2 object.
}
\description{
Creates a heatmap from the \code{ranks} element returned by
\code{summarize_top_units(..., include_ranks = TRUE)}. Rows are units, columns are
models, and cells show each model's rank for that unit.
}
\examples{
\dontrun{
top_books <- summarize_top_units(
  agg,
  outcome = "mean_delta_gap",
  item_by = "book",
  rank_within = "party",
  include_ranks = TRUE
)
plot_top_unit_heatmap(top_books$ranks, item_col = "book", facet_by = "party")
}
}
