% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_diagnostics.R
\name{tidy_forecast_aggregation}
\alias{tidy_forecast_aggregation}
\title{Tidy hierarchical forecast aggregation results}
\usage{
tidy_forecast_aggregation(
  aggregation,
  unit_by,
  outcomes = NULL,
  completion_col = "completion",
  prompt_col = "prompt_id",
  model_col = "model_id",
  family_col = "family"
)
}
\arguments{
\item{aggregation}{A named list returned by \code{\link[=aggregate_model_forecasts]{aggregate_model_forecasts()}}.}

\item{unit_by}{Character vector of columns identifying a forecast target.}

\item{outcomes}{Optional character vector of numeric outcome columns. When
omitted, numeric columns other than count and identifier columns are used.}

\item{completion_col, prompt_col, model_col, family_col}{Names of the identifier
columns used when the aggregation was made. \code{family_col} may be \code{NULL}.}
}
\value{
A tibble with \code{aggregation_level}, the available identifiers,
\code{outcome}, \code{estimate}, \code{contributor_type}, and \code{n_contributors}.
}
\description{
Converts the stage tables returned by \code{\link[=aggregate_model_forecasts]{aggregate_model_forecasts()}} into a
single long table.  The result is useful for diagnostics and plotting, and
retains the identifiers that apply at each aggregation level.
}
