% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_book_section_by_headings.R
\name{split_book_section_by_headings}
\alias{split_book_section_by_headings}
\title{Split a book section into chapter files using known headings}
\usage{
split_book_section_by_headings(
  input_file,
  chapter_titles,
  output_dir = file.path(dirname(input_file), "chapters"),
  chapter_ids = NULL,
  extension = "txt",
  overwrite = FALSE,
  include_heading = TRUE,
  allow_missing = FALSE
)
}
\arguments{
\item{input_file}{Character scalar. Path to the section \code{.txt} file.}

\item{chapter_titles}{Character vector of chapter titles, in the order they
appear in \code{input_file}.}

\item{output_dir}{Character scalar. Folder where chapter files should be
written. Defaults to a \verb{chapters/} subfolder beside \code{input_file}.}

\item{chapter_ids}{Optional character vector of file stems to use for output
files. Defaults to numbered slugs based on \code{chapter_titles}.}

\item{extension}{Character scalar output extension, without a leading dot.
Defaults to \code{"txt"}.}

\item{overwrite}{Logical scalar. If \code{TRUE}, replace existing output files.
Defaults to \code{FALSE}.}

\item{include_heading}{Logical scalar. If \code{TRUE}, keep each chapter heading
as the first line of its output file. Defaults to \code{TRUE}.}

\item{allow_missing}{Logical scalar. If \code{TRUE}, missing headings are skipped
with a warning. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per written chapter and columns for the chapter
title, output file, source line boundaries, and word count.
}
\description{
Splits one oversized section text file into chapter-level \code{.txt} files by
finding the ordered chapter headings supplied from a table of contents. This
is useful when uppercase headings alone are ambiguous because non-chapter
subsections use the same visual style.
}
