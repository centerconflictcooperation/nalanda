% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_identity_adherence.R
\name{summarize_identity_match_rates}
\alias{summarize_identity_match_rates}
\title{Summarize identity match rates by model}
\usage{
summarize_identity_match_rates(
  x,
  by = c("model", "identity"),
  compact = FALSE,
  expected_col = "identity",
  observed_col = "party"
)
}
\arguments{
\item{x}{A data frame or list-like object from a simulation workflow
containing \code{identity} and \code{party}.}

\item{by}{Character vector of columns to group by. Defaults to
\code{c("model", "identity")}.}

\item{compact}{Logical. If \code{TRUE}, return a wide one-row-per-group summary
with one rate column per requested identity plus a shared \code{n} column.}

\item{expected_col}{Character scalar naming the requested identity column.}

\item{observed_col}{Character scalar naming the model-reported identity
column.}
}
\value{
A tibble with counts and match rates, including \code{n_requested},
\code{n_match}, \code{adoption_rate}, \code{n_mismatch}, and \code{mismatch_rate}.
}
\description{
This helper converts raw identity-adoption output into one row per grouping
combination, reporting how often the model's reported identity matched the
requested one.
}
