% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompt_grid.R
\name{run_prompt_grid}
\alias{run_prompt_grid}
\title{Run a multi-model, multi-prompt grid}
\usage{
run_prompt_grid(
  data,
  content_col = "text",
  prompt_variants,
  response_type,
  model_config,
  id_col = NULL,
  smoke_n = NULL,
  dry_run = FALSE,
  output_dir = NULL,
  resume = TRUE,
  on_error = c("stop", "continue"),
  progress = interactive(),
  integration = getOption("nalanda.integration"),
  virtual_key = getOption("nalanda.virtual_key"),
  base_url = getOption("nalanda.base_url"),
  excerpt_chars = 200,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{data}{A data frame accepted by \code{\link[=run_structured_responses]{run_structured_responses()}}.}

\item{content_col, id_col, response_type}{Passed to
\code{\link[=run_structured_responses]{run_structured_responses()}}.}

\item{prompt_variants}{A named character vector of independent prompt
templates, or a data frame
with \code{prompt_id} and \code{prompt} columns. An optional logical \code{active} column
can disable prompt variants without deleting them.}

\item{model_config}{A character vector of model names, or a data frame with a
required \code{model} column. Supported optional columns are \code{model_id},
\code{family}, \code{integration}, \code{virtual_key}, \code{base_url}, \code{temperature},
\code{output_mode}, \code{seed}, \code{max_active}, \code{rpm}, \code{n_completions}, and \code{active}.
Missing settings use the same defaults as \code{\link[=run_structured_responses]{run_structured_responses()}}.}

\item{smoke_n}{Optional positive integer limiting the workflow to the first
\code{smoke_n} input rows. \code{smoke_n = 1} is useful before an expensive run.}

\item{dry_run}{Logical. If \code{TRUE}, return the call plan without constructing
a chat or making model calls.}

\item{output_dir}{Optional directory for one RDS checkpoint per completed
model-prompt-completion unit. Files contain raw, unaggregated results.}

\item{resume}{Logical. If \code{TRUE} and \code{output_dir} is supplied, compatible
completed units are read instead of rerun. Checkpoint identity includes
inputs, prompts, response type, and model settings.}

\item{on_error}{Either \code{"stop"} or \code{"continue"}. With \code{"continue"}, failed
units are recorded in the returned \code{errors} table and are not checkpointed,
so a later resumed run retries them.}

\item{progress}{Logical. Emit one concise progress message per completion.}

\item{integration, virtual_key, base_url, excerpt_chars, max_active, rpm}{Global
defaults passed to \code{\link[=run_structured_responses]{run_structured_responses()}}. A non-missing value in a model
configuration row overrides the corresponding global default.}
}
\value{
If \code{dry_run = TRUE}, the plan tibble. Otherwise, a list with
\code{results} (combined raw results), \code{plan}, \code{tasks}, and \code{errors}.
}
\description{
This function is a deliberately thin orchestration layer over
\code{\link[=run_structured_responses]{run_structured_responses()}}. It expands validated active model and prompt
configurations, runs each completion as an independently resumable unit,
and keeps model, family, prompt, and completion provenance in the raw
results. \code{\link[=run_text_analysis]{run_text_analysis()}} remains available as the text-annotation
use-case wrapper.
}
