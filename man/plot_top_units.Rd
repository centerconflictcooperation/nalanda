% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_top_units.R
\name{plot_top_units}
\alias{plot_top_units}
\title{Plot units that rank consistently high across models}
\usage{
plot_top_units(
  data,
  item_col = NULL,
  facet_by = NULL,
  top_n_items = NULL,
  title = "Units most consistently ranked highest",
  x_breaks = NULL,
  x_limits = NULL,
  caption = NULL,
  show_top_n_label = TRUE
)
}
\arguments{
\item{data}{Output of \code{\link[=summarize_top_units]{summarize_top_units()}}.}

\item{item_col}{Character. Column identifying the ranked item. If \code{NULL},
the function tries to infer \code{"book"} or \code{"book_id"}.}

\item{facet_by}{Optional character vector of columns to facet by, e.g.
\code{"party"}.}

\item{top_n_items}{Optional integer. If supplied, keep only the best
\code{top_n_items} per facet, based on \code{mean_rank}.}

\item{title}{Optional plot title.}

\item{x_breaks}{Optional numeric vector of x-axis breaks. If \code{NULL}, integer
rank breaks are shown by default.}

\item{x_limits}{Optional numeric vector of length 2. If \code{NULL}, limits are
chosen from the displayed mean ranks.}

\item{caption}{Optional plot caption. If \code{NULL}, a caption explaining the
point-size and top-N label encodings is generated when possible.}

\item{show_top_n_label}{Logical. If \code{TRUE} (default), label points with the
number of models placing the item in the top \code{N}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a dot plot from \code{\link[=summarize_top_units]{summarize_top_units()}} output. Units are ordered by
average rank across models, point size shows the mean score, and text labels
show how many models placed the unit in the top \code{N}.
}
\examples{
\dontrun{
top_books <- summarize_top_units(
  agg,
  outcome = "mean_delta_gap",
  item_by = "book",
  rank_within = "party"
)
plot_top_units(top_books, item_col = "book", facet_by = "party")
}
}
