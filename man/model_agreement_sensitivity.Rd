% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_agreement.R
\name{model_agreement_sensitivity}
\alias{model_agreement_sensitivity}
\title{Summarize model agreement across analysis levels}
\usage{
model_agreement_sensitivity(
  data,
  outcome = "mean_outcome",
  model_col = "model",
  analyses = NULL,
  metrics = c("icc", "kendall_w"),
  format = c("wide", "long"),
  digits = 2,
  drop_missing = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per model-by-unit combination.}

\item{outcome}{Character string naming the score column (default
\code{"mean_outcome"}).}

\item{model_col}{Character string naming the model column (default
\code{"model"}).}

\item{analyses}{Optional named list defining analyses to run. Each element
should be a list with \code{unit_by} and, optionally, \code{group_by}. If \code{NULL}, a
default set is inferred from available book, chapter, and party/group
columns.}

\item{metrics}{Character vector of metrics to compute. One or both of
\code{"icc"} and \code{"kendall_w"} (default both).}

\item{format}{Character. \code{"wide"} returns one row per analysis/subgroup with
ICC and Kendall's W side by side; \code{"long"} returns the stacked
\code{\link[=model_agreement]{model_agreement()}} results with analysis labels.}

\item{digits}{Integer. Number of decimal places used in the formatted wide
table.}

\item{drop_missing}{Logical. Whether to drop rows with missing model, unit,
or grouping identifiers before computing each analysis (default \code{TRUE}).}
}
\value{
A tibble. With \code{format = "wide"}, columns include \verb{Analysis level},
\code{Subgroup}, \verb{N models}, \verb{N units}, \code{ICC}, and \verb{Kendall's W}.
}
\description{
Builds a slide-ready sensitivity table by running \code{\link[=model_agreement]{model_agreement()}} across
several substantively useful unit definitions (for example book-level,
chapter-level, and party-specific agreement). Lower-level rows are
aggregated with an NA-safe mean before each agreement calculation, so skipped
chapters do not turn an entire book mean into \code{NA}.
}
\examples{
\dontrun{
model_agreement_sensitivity(
  agg,
  outcome = "mean_delta_gap",
  model_col = "model"
)
}
}
