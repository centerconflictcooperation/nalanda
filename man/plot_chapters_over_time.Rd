% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_chapters_over_time.R
\name{plot_chapters_over_time}
\alias{plot_chapters_over_time}
\title{Plot chapters over time (multi-timepoint means)}
\usage{
plot_chapters_over_time(
  chapters,
  dv = "delta_gap",
  group = "book",
  x_label = "Chapter",
  y_label = "Simulated scores",
  plot_title = TRUE,
  plot_subtitle = "",
  append_model_info = TRUE,
  ci_type = "between",
  legend.position = "bottom",
  groups.order = "decreasing",
  text_size = 20,
  line_width = 3,
  point_size = 4,
  reverse_score = FALSE,
  error_bars = TRUE,
  neutrality_line = TRUE,
  point_images = NULL,
  image_size = 0.04,
  image_nudge_x = 0,
  image_nudge_y = 0,
  image_jitter_width = 0,
  image_jitter_height = 0,
  facet = NULL,
  facets.order = "increasing"
)
}
\arguments{
\item{chapters}{A data frame or list of processed simulation rows, typically
returned by \code{\link[=compute_run_ai_metrics]{compute_run_ai_metrics()}}, containing columns \code{book},
\code{chapter}, and the desired \code{dv}.}

\item{dv}{Character. Name of the column to plot as the dependent variable (default: "pre_post_outgroup_difference").}

\item{group}{The group by which to plot the variable}

\item{x_label}{Character. X-axis label.}

\item{y_label}{Character. Y-axis label.}

\item{plot_title}{Logical. Whether to include a title.}

\item{plot_subtitle}{Optional plot subtitle.}

\item{append_model_info}{Logical. If \code{TRUE} (default), append model and
temperature attributes to the subtitle (or create one if none is provided).}

\item{ci_type}{Character. Type of confidence interval to pass to \code{rempsyc::plot_means_over_time}.}

\item{legend.position}{Position for legend.}

\item{groups.order}{Specifies the desired display order of the groups
on the legend. Either provide the levels directly, or a string: "increasing"
or "decreasing", to order based on the average value of the variable on the
y axis, or "string.length", to order from the shortest to the longest
string (useful when working with long string names). "Defaults to "decreasing".}

\item{text_size}{Numeric. Base text size for axis/title text.}

\item{line_width}{Numeric. Line thickness used in \code{geom_line()}.
Defaults to 3. Can be reduced for publication figures or increased
for presentation slides.}

\item{point_size}{Numeric. Point size used in \code{geom_point()}.
Defaults to 4. Adjust to improve readability depending on output format.}

\item{reverse_score}{Logical. Whether to reverse score scale.}

\item{error_bars}{Logical. Show error bars.}

\item{neutrality_line}{Logical. Add a horizontal neutrality line at 50.}

\item{point_images}{Optional named list mapping group levels to image file
paths (PNG recommended). When supplied, the point markers are replaced with
the corresponding images, and the legend labels are updated to show the
matching image alongside the group name when \code{ggtext} is installed. Example:
\code{list(Democrat = "logos/dem.png", Republican = "logos/rep.png")}.}

\item{image_size}{Numeric. Size of images when \code{point_images} is used.
Passed to \code{\link[ggimage:geom_image]{ggimage::geom_image()}}. Defaults to \code{0.04}.}

\item{image_nudge_x}{Numeric. Horizontal offset applied to point images only.
Defaults to \code{0}.}

\item{image_nudge_y}{Numeric. Vertical offset applied to point images only.
Defaults to \code{0}.}

\item{image_jitter_width}{Numeric. Horizontal jitter width applied to point
images only. Defaults to \code{0}.}

\item{image_jitter_height}{Numeric. Vertical jitter height applied to point
images only. Defaults to \code{0}.}

\item{facet}{The variable by which to facet grid.}

\item{facets.order}{Specifies the desired display order of facet panels.
Either provide the levels directly, or a string: "increasing" or
"decreasing", to order panels based on the average value of the
y variable, or "string.length" to order panels by facet label length.
Defaults to "increasing".}
}
\value{
A ggplot2 object.
}
\description{
Create a plot showing means over chapter timepoints using rempsyc::plot_means_over_time
for the wide-format response variables.
}
