% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_agreement.R
\name{summarize_top_units}
\alias{summarize_top_units}
\title{Summarize units that rank consistently high across models}
\usage{
summarize_top_units(
  data,
  outcome = "mean_outcome",
  item_by = "book_id",
  rank_within = NULL,
  model_col = "model",
  top_n = 3,
  higher_is_better = TRUE,
  standardize = c("z", "none", "minmax", "max"),
  include_ranks = FALSE,
  drop_missing = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per model-by-unit combination.}

\item{outcome}{Character string naming the score column (default
\code{"mean_outcome"}).}

\item{item_by}{Character vector identifying the items to rank, e.g. \code{"book"}
or \code{"book_id"}.}

\item{rank_within}{Optional character vector defining separate ranking
contexts, e.g. \code{"party"} to rank books separately within party.}

\item{model_col}{Character string naming the model column (default
\code{"model"}).}

\item{top_n}{Integer. Number of top-ranked items to count for each model.}

\item{higher_is_better}{Logical. If \code{TRUE} (default), larger outcome values
receive better ranks. If \code{FALSE}, smaller values receive better ranks.}

\item{standardize}{Character. How to standardize item scores within each
model before computing cross-model mean scores. \code{"z"} (default) centers
and scales scores within model; \code{"none"} keeps raw scores; \code{"minmax"}
rescales scores within model to 0--1; \code{"max"} divides scores within model
by that model's maximum absolute score. Ranks are unchanged by monotonic
standardization, but \code{mean_score} and point sizes in \code{\link[=plot_top_units]{plot_top_units()}} use
the standardized scores.}

\item{include_ranks}{Logical. If \code{TRUE}, return a list with both the summary
table and the model-level ranks. If \code{FALSE} (default), return only the
summary table.}

\item{drop_missing}{Logical. Whether to drop rows with missing model, item,
or ranking-context identifiers before aggregating (default \code{TRUE}).}
}
\value{
A tibble, or a list with \code{summary} and \code{ranks} when
\code{include_ranks = TRUE}.

The summary table contains:
\describe{
\item{\code{rank_within} columns}{Optional grouping columns used to define
separate ranking contexts, such as party.}
\item{\code{item_by} columns}{The ranked item identifiers, such as book.}
\item{\code{mean_score}}{Mean outcome score for the item across models.}
\item{\code{score_scale}}{The score standardization method used for
\code{mean_score}.}
\item{\code{mean_rank}}{Average rank of the item across models. Lower values
indicate more consistently high-ranked items when
\code{higher_is_better = TRUE}.}
\item{\code{overall_mean_rank}}{When \code{rank_within} is supplied, the item's
average rank computed without those ranking contexts. This preserves a
common item order for subgroup displays.}
\item{\code{median_rank}}{Median rank of the item across models.}
\item{\code{top_n_models}}{Number of models that ranked the item within the
top \code{top_n} items in its ranking context. For example, if
\code{top_n = 3} and \code{top_n_models = 4}, then 4 models placed that item in
their top 3.}
\item{\code{n_models}}{Number of models with non-missing ranks for the item.}
\item{\code{top_n}}{The top-N threshold used to compute \code{top_n_models}.}
\item{\code{top_n_label}}{Compact display label combining \code{top_n_models} and
\code{n_models}, such as \code{"4/5"}.}
}

When \code{include_ranks = TRUE}, the \code{ranks} table contains one row per
model-by-item combination, including \code{score}, \code{rank}, and \code{top_n}.
}
\description{
Aggregates lower-level rows to a chosen unit level, ranks units within each
model, and summarizes which units most consistently appear near the top
across models. This is useful for questions such as "Which books
consistently have the strongest effects across models?"
}
\examples{
\dontrun{
summarize_top_units(
  agg,
  outcome = "mean_delta_gap",
  item_by = "book",
  rank_within = "party",
  model_col = "model",
  top_n = 3
)
}
}
