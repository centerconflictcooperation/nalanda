% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_treatment.R
\name{simulate_treatment}
\alias{simulate_treatment}
\title{Simulate a generic multi-turn treatment workflow}
\usage{
simulate_treatment(
  intervention_text = "",
  prompt,
  response_type,
  output_mode = c("structured", "text"),
  groups = NULL,
  context_text = NULL,
  n_simulations = 1,
  temperature = 0,
  seed = 42,
  model = "gemini-2.5-flash-lite",
  integration = getOption("nalanda.integration"),
  virtual_key = getOption("nalanda.virtual_key"),
  base_url = getOption("nalanda.base_url"),
  excerpt_chars = 200,
  checkpoint_dir = NULL,
  checkpoint_prefix = "simulate_treatment",
  save_dir = NULL,
  save_prefix = "results"
)
}
\arguments{
\item{intervention_text}{A single character string or a nested list of
intervention texts. This is mapped internally onto the same job grid used
by \code{\link[=run_ai_on_chapters]{run_ai_on_chapters()}}. Defaults to \code{""}, which is useful when the full
treatment is already encoded in \code{prompt} and/or \code{context_text}.}

\item{prompt}{Character vector of prompt templates. Each element defines one
ordered turn in the same conversation, not an independent prompt variant.
Prompt templates may include \code{{intervention_text}}, \code{{identity}}, and
\code{{group}} placeholders.}

\item{response_type}{An \code{ellmer} structured type specification applied to all
turns (for example \code{ellmer::type_object(score = ellmer::type_number())}).}

\item{output_mode}{Character. \code{"structured"} (default) uses the backend's
structured-output support. \code{"text"} is a compatibility mode for models
that do not support structured outputs (for example some Anthropic models):
nalanda appends strict JSON-only instructions to the prompt, calls the
model as free text, then parses the JSON back into the same tabular fields.
Text mode is best-effort and stores the original model reply in
\code{raw_response}.}

\item{groups}{Optional character vector of group labels. If supplied, the
full prompt sequence is rerun for each group identity.}

\item{context_text}{Optional character scalar or vector. If provided, it is
prepended to every turn for the corresponding group. Scalar values are
recycled across groups, and \code{{identity}} is expanded when present.}

\item{n_simulations}{Integer. Number of repeated simulations per
intervention per identity.}

\item{temperature}{Numeric. Sampling temperature passed to the chat backend.}

\item{seed}{Integer. Random seed for reproducibility (incremented for each
simulation index).}

\item{model}{Character. Model name for the chat backend.}

\item{integration}{Optional Portkey/gateway route slug. If supplied and
\code{model} is not fully-qualified, nalanda will build
\code{"@{integration}/{model}"}. Use a route returned by
\code{ellmer::models_portkey(base_url = "https://ai-gateway.apps.cloud.rt.nyu.edu/v1/")}
when working with the NYU gateway.}

\item{virtual_key}{Optional legacy virtual key. If supplied and \code{model} is
not fully-qualified, nalanda will build \code{"@{virtual_key}/{model}"}.}

\item{base_url}{Character. Base URL for API calls.}

\item{excerpt_chars}{Integer. Number of intervention-text characters to
retain in stored prompt previews.}

\item{checkpoint_dir}{Optional directory. If supplied, each completed
treatment/identity/simulation unit is saved as its own \code{.Rds} file as soon
as it finishes. If the same call is rerun with the same \code{checkpoint_dir},
\code{checkpoint_prefix}, model, treatments, groups, and simulations, completed
units are loaded from disk and skipped.}

\item{checkpoint_prefix}{Character scalar used at the start of checkpoint
filenames when \code{checkpoint_dir} is supplied.}

\item{save_dir}{Optional directory. If supplied, each intervention collection
is saved as one \code{.Rds} file as soon as all of its treatments, identities,
and simulations finish.}

\item{save_prefix}{Character scalar used in book-level filenames when
\code{save_dir} is supplied. Files are named \verb{\{save_prefix\}_\{book\}.Rds}.}
}
\value{
A tibble of raw turn-level responses, or a named list of tibbles
(one per book/intervention collection). Each row includes \code{treatment},
\code{sim}, \code{identity}, \code{turn_index}, \code{turn_type}, and one column per field
returned by \code{response_type}, plus stored prompt previews and metadata
columns.
}
\description{
This function provides a simpler, prompt-first interface for running one or
more turns against an intervention text. Each element of \code{prompt} defines one
turn in the chat sequence. When \code{groups} is supplied, the same prompt
sequence is repeated for each group identity; groups do not create additional
turns. For independent alternative prompts that must start fresh
conversations, use \code{\link[=run_prompt_grid]{run_prompt_grid()}} instead.
}
\examples{
\dontrun{
simulate_treatment(
  intervention_text = "A short passage about people working together.",
  prompt = c(
    "Read the following text:\n\n{intervention_text}\n\nRate its readability from 0 to 100."
  ),
  response_type = ellmer::type_object(
    score = ellmer::type_number()
  ),
  n_simulations = 2,
  temperature = 0,
  seed = 42
)

simulate_treatment(
  groups = c("South African", "Danish"),
  context_text = "You are simulating an adult who identifies as {identity}.",
  prompt = c(
    climate_belief = paste(
      "Generally speaking, do you usually think of yourself as Danish or South African?",
      "On a scale from 0 to 100, how accurate do you think this statement is?",
      "Statement: Human activities are causing climate change"
    )
  ),
  response_type = ellmer::type_object(
    rating = ellmer::type_number()
  ),
  n_simulations = 2,
  temperature = 0,
  seed = 42
)
}
}
