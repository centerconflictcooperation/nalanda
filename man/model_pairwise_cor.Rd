% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_agreement.R
\name{model_pairwise_cor}
\alias{model_pairwise_cor}
\title{Pairwise model correlations}
\usage{
model_pairwise_cor(
  data,
  outcome = "mean_outcome",
  unit_by = c("book_id", "chapter_id", "group"),
  group_by = NULL,
  model_col = "model",
  methods = c("pearson", "spearman")
)
}
\arguments{
\item{data}{A data frame with one row per model-by-unit combination.}

\item{outcome}{Character string naming the score column (default
\code{"mean_outcome"}).}

\item{unit_by}{Character vector of columns that jointly identify a unit
(default \code{c("book_id", "chapter_id", "group")}).}

\item{group_by}{Optional character vector. If provided, agreement metrics
are computed separately within each level of these columns (e.g.,
\code{"group"} to get separate estimates for Democrats and Republicans).}

\item{model_col}{Character string naming the model column (default
\code{"model"}).}

\item{methods}{Character vector of correlation types. One or both of
\code{"pearson"} and \code{"spearman"} (default both).}
}
\value{
A tibble with columns: any \code{group_by} columns, plus \code{model_a},
\code{model_b}, \code{method}, \code{correlation}, and \code{n_units}.
}
\description{
Computes Pearson and/or Spearman correlations between every pair of models
on a shared set of units. This is a diagnostic complement to the omnibus
metrics in \code{\link[=model_agreement]{model_agreement()}}: it reveals \emph{which} models diverge.
}
\examples{
\dontrun{
pw <- model_pairwise_cor(agg, outcome = "mean_rating",
  unit_by = c("book_id", "chapter_id", "group"))
plot_model_agreement(pw, type = "heatmap")
}
}
