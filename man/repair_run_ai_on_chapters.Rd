% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repair_run_ai_on_chapters.R
\name{repair_run_ai_on_chapters}
\alias{repair_run_ai_on_chapters}
\title{Repair failed chapter simulation units}
\usage{
repair_run_ai_on_chapters(
  x,
  book_texts,
  groups,
  context_text,
  question_text,
  output_mode = c("structured", "text"),
  temperature = NULL,
  seed = 42,
  model = NULL,
  integration = getOption("nalanda.integration"),
  virtual_key = getOption("nalanda.virtual_key"),
  base_url = getOption("nalanda.base_url"),
  excerpt_chars = 200,
  on_error = c("stop", "skip")
)
}
\arguments{
\item{x}{A result from \code{\link[=run_ai_on_chapters]{run_ai_on_chapters()}} or a path to its \code{.Rds} file.}

\item{book_texts}{A single character (one chapter) or a nested list of
books -> chapters as returned by \code{read_book_texts()}.}

\item{groups}{Character vector of group labels (length >= 2). These are the
groups being compared. Example: \code{c("Democrat", "Republican")}.}

\item{context_text}{Character. Either:
\itemize{
\item A scalar template containing \code{{identity}}, which will be expanded
once for each group (e.g., \code{"You are simulating an American adult who politically identifies as a {identity}."}), or
\item A character vector of length equal to \code{length(groups)}, where each
element is the full context for the corresponding group identity.
}}

\item{question_text}{Character scalar. A question template containing the
placeholder \code{{group}}, which will be replaced with each group label.
Example: \code{"On a scale from 0 to 100, how warmly do you feel towards {group}s?"}}

\item{output_mode}{Character. \code{"structured"} (default) uses the backend's
structured-output support. \code{"text"} is a compatibility mode for models
that do not support structured outputs (for example some Anthropic models):
nalanda appends strict JSON-only instructions to the prompt, calls the
model as free text, then parses the JSON back into the same fields used by
the rest of the pipeline. Text mode is best-effort and stores the original
model reply in \code{raw_response}.}

\item{temperature}{Numeric. Sampling temperature passed to the chat backend.}

\item{seed}{Integer. Random seed for reproducibility (incremented for each
simulation).}

\item{model}{Character. Model name for the chat backend (for example,
\code{"gemini-2.5-flash-lite"}). The value is passed directly to
\code{ellmer::chat_portkey(model = ...)}.}

\item{integration}{Optional Portkey/gateway route slug. Should look like
\code{"vertexai"} or another route returned by
\code{ellmer::models_portkey(base_url = "https://ai-gateway.apps.cloud.rt.nyu.edu/v1/")}.
If supplied and \code{model} is not fully-qualified (does not start with \code{"@"}),
nalanda will build \code{"@{integration}/{model}"}. In some gateways this slug
is not the upstream provider name. When available, a fully-qualified model
string such as \code{"@gpt-5-mini/gpt-5-mini"} is the most reliable option.
When both \code{nalanda.integration} and \code{nalanda.virtual_key} options are set
and neither argument is supplied, \code{integration} is preferred.}

\item{virtual_key}{Optional legacy virtual key. Should look like
\code{"gemini-8c2498"} or similar. If supplied and \code{model} is not
fully-qualified, nalanda will build \code{"@{virtual_key}/{model}"}.
Use either \code{integration} or \code{virtual_key}, not both when explicitly
supplying function arguments.}

\item{base_url}{Character. Base URL for API calls.}

\item{excerpt_chars}{Integer. Number of chapter characters to retain in the
stored post-prompt preview shown in results.}

\item{on_error}{Error policy for the repair attempts.}
}
\value{
A repaired result with the same outer shape as \code{x}. Attributes
\code{repaired_units} and \code{non_retryable_units} identify the units selected for
retry and those retained because of content-policy failures, respectively.
}
\description{
Reads a saved result (or accepts one in memory), reruns retryable failed
simulation units, and returns a copy with those units replaced. Permanent
Azure content-policy failures are retained without being retried. The source
file is never modified; inspect the result and call \code{saveRDS()} yourself.
}
