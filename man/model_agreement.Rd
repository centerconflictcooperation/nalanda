% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_agreement.R
\name{model_agreement}
\alias{model_agreement}
\title{Compute inter-model agreement}
\usage{
model_agreement(
  data,
  outcome = "mean_outcome",
  unit_by = c("book_id", "chapter_id", "group"),
  group_by = NULL,
  model_col = "model",
  metrics = c("icc", "kendall_w")
)
}
\arguments{
\item{data}{A data frame with one row per model-by-unit combination.}

\item{outcome}{Character string naming the score column (default
\code{"mean_outcome"}).}

\item{unit_by}{Character vector of columns that jointly identify a unit
(default \code{c("book_id", "chapter_id", "group")}).}

\item{group_by}{Optional character vector. If provided, agreement metrics
are computed separately within each level of these columns (e.g.,
\code{"group"} to get separate estimates for Democrats and Republicans).}

\item{model_col}{Character string naming the model column (default
\code{"model"}).}

\item{metrics}{Character vector of metrics to compute. One or both of
\code{"icc"} and \code{"kendall_w"} (default both).}
}
\value{
A tibble with columns: any \code{group_by} columns, plus
\describe{
\item{metric}{\code{"icc"} or \code{"kendall_w"}.}
\item{value}{The agreement statistic (0--1 scale).}
\item{interpretation}{Qualitative label (e.g., "good", "moderate").}
\item{n_models}{Number of models (raters).}
\item{n_units}{Number of units (targets).}
\item{p_value}{p-value for the statistic (F-test for ICC, chi-squared
approximation for Kendall's W).}
}
}
\description{
Quantifies how consistently different AI models score the same units using
ICC(2,1) (intraclass correlation, absolute agreement) and/or Kendall's W
(coefficient of concordance). Models produce continuous scores on a 1--100
scale; this function operates on those raw scores (typically after
aggregating simulation runs via \code{\link[=aggregate_simulations]{aggregate_simulations()}}).
}
\details{
Each model is treated as a \strong{rater} and each unique combination of
\code{unit_by} columns as a \strong{target}. ICC captures agreement in both level and
rank order; Kendall's W converts the continuous scores to ranks internally
and assesses rank-order concordance only.
\subsection{Which metric to report?}{
\itemize{
\item \strong{ICC(2,1)} is the primary recommendation for continuous scores. It
penalises models that systematically differ in level \strong{and} in rank
ordering. Interpret with Cicchetti (1994) cut-offs: < .40 poor,
.40--.59 fair, .60--.74 good, >= .75 excellent.
\item \strong{Kendall's W} converts the continuous 1--100 scores to ranks and asks
only whether models rank the units the same way. Useful when the
absolute scale is arbitrary or when the researcher cares about ordinal
agreement (e.g., "which book scored highest?") rather than exact score
match.
}

For a quick "single consistency score," report ICC. Add Kendall's W as a
supplementary rank-agreement check.
}

\subsection{Aggregation guidance}{

Always aggregate simulation runs first via \code{\link[=aggregate_simulations]{aggregate_simulations()}}.
Failing to do so inflates \emph{n} and distorts agreement estimates.
}
}
\examples{
\dontrun{
# After aggregating simulations
agg <- aggregate_simulations(sim_data, outcome = "rating",
  by = c("model", "book_id", "chapter_id", "group"))

# Overall agreement
model_agreement(agg, outcome = "mean_rating",
  unit_by = c("book_id", "chapter_id", "group"))

# Agreement by political group
model_agreement(agg, outcome = "mean_rating",
  unit_by = c("book_id", "chapter_id"),
  group_by = "group")
}
}
