% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_agreement.R
\name{summarize_model_correlations}
\alias{summarize_model_correlations}
\title{Summarize pairwise model correlations}
\usage{
summarize_model_correlations(data, method = NULL, digits = 2)
}
\arguments{
\item{data}{Output of \code{\link[=model_pairwise_cor]{model_pairwise_cor()}}.}

\item{method}{Optional character. If supplied, keep only one correlation
method, e.g. \code{"pearson"} or \code{"spearman"}.}

\item{digits}{Integer. Number of decimal places used in the display \code{label}.}
}
\value{
A tibble with any subgroup columns from \code{data}, plus \code{method},
\code{mean_correlation}, \code{median_correlation}, \code{min_correlation},
\code{max_correlation}, \code{n_pairs}, \code{most_aligned_model},
\code{most_aligned_correlation}, and \code{label}.
}
\description{
Condenses the output of \code{\link[=model_pairwise_cor]{model_pairwise_cor()}} into one row per correlation
method and subgroup. The summary includes the average pairwise correlation
and the "most aligned" model, defined as the model with the highest average
correlation with all other models. This is useful for adding a small
headline annotation to correlation-matrix slides.
}
\examples{
\dontrun{
pw <- model_pairwise_cor(agg, outcome = "mean_rating",
  unit_by = c("book_id", "chapter_id", "group"))
summarize_model_correlations(pw, method = "pearson")
}
}
