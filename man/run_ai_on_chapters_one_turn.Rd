% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ai_on_chapters_one_turn.R
\name{run_ai_on_chapters_one_turn}
\alias{run_ai_on_chapters_one_turn}
\title{Run AI model on book chapters with a single prompt per simulation}
\usage{
run_ai_on_chapters_one_turn(
  book_texts,
  groups,
  context_text,
  question_text,
  n_simulations = 1,
  temperature = 0,
  seed = 42,
  model = "gemini-2.5-flash-lite",
  integration = getOption("nalanda.integration"),
  virtual_key = getOption("nalanda.virtual_key"),
  base_url = getOption("nalanda.base_url"),
  excerpt_chars = 200,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{book_texts}{A single character (one chapter) or a nested list of
books -> chapters as returned by \code{read_book_texts()}.}

\item{groups}{Character vector of group labels (length >= 2). These are the
groups being compared. Example: \code{c("Democrat", "Republican")}.}

\item{context_text}{Character. Either:
\itemize{
\item A scalar template containing \code{{identity}}, which will be expanded
once for each group.
\item A character vector of length equal to \code{length(groups)}, where each
element is the full context for the corresponding group identity.
}}

\item{question_text}{Character scalar. A question template containing the
placeholder \code{{group}}, which will be replaced with each group label in
per-group mode.}

\item{n_simulations}{Integer. Number of repeated simulations per chapter per
identity.}

\item{temperature}{Numeric. Sampling temperature passed to the chat backend.}

\item{seed}{Integer. Random seed for reproducibility. As in
\code{\link[=run_ai_on_chapters]{run_ai_on_chapters()}}, the seed varies by simulation index only, so all
chapters and identities within the same \code{sim} share the same seed.}

\item{model}{Character. Model name for the chat backend.}

\item{integration}{Optional Portkey/gateway route slug. If supplied and
\code{model} is not fully-qualified, nalanda will build
\code{"@{integration}/{model}"}. Use a route returned by
\code{ellmer::models_portkey(base_url = "https://ai-gateway.apps.cloud.rt.nyu.edu/v1/")}
when working with the NYU gateway. When both \code{nalanda.integration} and
\code{nalanda.virtual_key} options are set and neither argument is supplied,
\code{integration} is preferred.}

\item{virtual_key}{Optional legacy virtual key. If supplied and \code{model} is
not fully-qualified, nalanda will build \code{"@{virtual_key}/{model}"}.
Use either \code{integration} or \code{virtual_key}, not both when explicitly
supplying function arguments.}

\item{base_url}{Character. Base URL for API calls.}

\item{excerpt_chars}{Integer. Number of chapter characters to retain in the
stored prompt preview shown in results.}

\item{max_active}{Integer. Maximum number of concurrent requests passed to
\code{\link[ellmer:parallel_chat_structured]{ellmer::parallel_chat_structured()}}.}

\item{rpm}{Integer. Requests-per-minute cap passed to
\code{\link[ellmer:parallel_chat_structured]{ellmer::parallel_chat_structured()}}.}
}
\value{
A tibble of raw single-turn ratings, or a named list of tibbles (one
per book). Each row is one rating observation and includes \code{chapter},
\code{sim}, \code{identity}, \code{turn_index}, \code{turn_type}, \code{target_group}, and
\code{rating}, plus prompt and metadata columns. Use
\code{\link[=compute_run_ai_metrics_one_turn]{compute_run_ai_metrics_one_turn()}} to derive chapter-level one-turn
summaries.
}
\description{
This function implements a one-turn design where identity context, chapter
text, and the rating question are combined into a single prompt. Independent
prompts are executed in parallel with \code{\link[ellmer:parallel_chat_structured]{ellmer::parallel_chat_structured()}}.
}
