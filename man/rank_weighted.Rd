% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_weighted.R
\name{rank_weighted}
\alias{rank_weighted}
\title{Rank rows using a weighted rubric}
\usage{
rank_weighted(data, weights, normalize = TRUE, decreasing = TRUE)
}
\arguments{
\item{data}{A data frame.}

\item{weights}{Named numeric vector of weights. Names must match columns in
\code{data}, and weights must sum to 1.}

\item{normalize}{Logical. If \code{TRUE} (default), selected variables are scaled
to \verb{[0, 1]} using min-max normalization before weighting.}

\item{decreasing}{Logical. If \code{TRUE} (default), rows are sorted from highest
to lowest \code{weighted_score}.}
}
\value{
A tibble containing all original columns plus \code{weighted_score},
sorted by score.
}
\description{
Compute a weighted score across selected numeric columns and return the input
data with a final \code{weighted_score}, sorted by score.
}
\examples{
book_summary <- summarize_chapter_scores(
  toy_sim_results,
  aggregate_level = "book"
)
rank_weighted(
  book_summary,
  weights = c(mean_delta_outgroup = 0.6, mean_delta_gap = 0.4)
)
}
