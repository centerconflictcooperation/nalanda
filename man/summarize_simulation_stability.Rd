% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_simulation_stability.R
\name{summarize_simulation_stability}
\alias{summarize_simulation_stability}
\title{Summarize simulation stability across chapters}
\usage{
summarize_simulation_stability(x, by = "party", metrics = NULL, tol = 0)
}
\arguments{
\item{x}{A data frame or list-like object. This can be raw simulation metrics
(for example from \code{\link[=compute_run_ai_metrics]{compute_run_ai_metrics()}}) or chapter summaries from
\code{\link[=summarize_chapter_scores]{summarize_chapter_scores()}}.}

\item{by}{Character vector of columns used for the compact summary. Defaults
to \code{"party"}.}

\item{metrics}{Optional character vector of metric names to inspect without
the \code{sd_} prefix. Defaults to the four core ratings:
\code{pre_ingroup}, \code{pre_outgroup}, \code{post_ingroup}, and \code{post_outgroup}.}

\item{tol}{Numeric tolerance for treating an SD as zero. Defaults to \code{0}.}
}
\value{
A tibble with one row per grouping combination, including the number
of assessed units (\code{n_units}), the proportion of units showing any
pre-period variation, the proportion showing any post-period variation,
and an overall \code{all_stable} flag.
}
\description{
This helper provides a compact bird's-eye view of where repeated simulation
runs vary across chapters, books, parties, or models. It reuses the
simulation-level SD columns from \code{\link[=summarize_chapter_scores]{summarize_chapter_scores()}} and reports how
often each metric showed non-zero variation within the requested grouping.
}
\details{
If \code{x} is already output from \code{\link[=summarize_chapter_scores]{summarize_chapter_scores()}}, the function uses
it directly. Otherwise, it first computes chapter-level summaries with
\code{by_party = TRUE}, because party-specific stability is the most common
diagnostic use case.

Groups with only one simulation row have \code{NA} SD values from
\code{\link[stats:sd]{stats::sd()}}. Those groups are treated as not testable for variability and
are excluded from the variation counts.
}
\examples{
stability <- summarize_simulation_stability(toy_sim_results)
stability

summarize_simulation_stability(
  toy_sim_results,
  by = c("model", "party")
)
}
