% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_diagnostics.R
\name{compare_forecast_aggregations}
\alias{compare_forecast_aggregations}
\title{Compare estimates from two forecast aggregations}
\usage{
compare_forecast_aggregations(
  first,
  second,
  stage = "consensus",
  unit_by,
  outcomes,
  labels = c("first", "second"),
  scale_width = NULL
)
}
\arguments{
\item{first, second}{Named aggregation lists to compare.}

\item{stage}{Aggregation stage: one of \code{"prompt"}, \code{"model"}, \code{"family"},
or \code{"consensus"}.}

\item{unit_by}{Character vector identifying a forecast target.}

\item{outcomes}{Character vector of outcome columns.}

\item{labels}{Two names for the estimate columns in the result.}

\item{scale_width}{Optional scale-width specification: a character column
present in both selected stage tables, or a data frame with \code{unit_by},
optional \code{outcome}, and a numeric \code{scale_width} column.}
}
\value{
A tibble with both estimates, \code{difference} (\code{second - first}),
\code{absolute_difference}, and optional scale-normalized differences.
}
\description{
Aligns a selected stage from two compatible outputs of
\code{\link[=aggregate_model_forecasts]{aggregate_model_forecasts()}} and reports signed and absolute differences.
Unlike a permissive join, this helper errors when either result has missing
or duplicate unit--outcome identities.
}
