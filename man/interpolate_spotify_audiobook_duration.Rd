% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate_spotify_audiobook_duration.R
\name{interpolate_spotify_audiobook_duration}
\alias{interpolate_spotify_audiobook_duration}
\title{Interpolate Spotify audiobook duration from text size}
\usage{
interpolate_spotify_audiobook_duration(
  reference,
  target = NULL,
  duration_col,
  books_path = NULL,
  target_book = NULL,
  book_col = "book",
  extension = "txt",
  reference_book_col = NULL,
  size_col = NULL,
  words_col = NULL,
  file_col = NULL,
  text_col = NULL,
  measure = c("file_size", "word_count"),
  duration_unit = c("seconds", "minutes", "hours", "hms"),
  output_unit = c("minutes", "seconds", "hours", "hms"),
  method = c("ratio", "lm")
)
}
\arguments{
\item{reference}{Data frame with known Spotify durations and, unless
\code{books_path} is supplied, a text-size predictor.}

\item{target}{Data frame with chapters or books to estimate. When
\code{books_path} and \code{target_book} are supplied, this can be left as \code{NULL}.}

\item{duration_col}{Character scalar. Column in \code{reference} containing known
Spotify duration.}

\item{books_path}{Character scalar or \code{NULL}. Optional folder containing one
subfolder per book, with chapter text files inside each book folder.}

\item{target_book}{Character vector or \code{NULL}. Book folder name(s) to
estimate when \code{books_path} is supplied.}

\item{book_col}{Character scalar. Book identifier column in \code{reference}.}

\item{extension}{Character scalar. File extension to read from \code{books_path}.}

\item{reference_book_col}{Character scalar or \code{NULL}. Optional book identifier
in \code{reference}. When supplied, the predictor is summed within each book and
\code{duration_col} must contain one unique duration per book. Use this when
reference rows are chapter-level but Spotify durations are book-level.}

\item{size_col}{Character scalar or \code{NULL}. Column containing file sizes in
bytes. Use this when \code{measure = "file_size"}.}

\item{words_col}{Character scalar or \code{NULL}. Column containing word counts.
Use this when \code{measure = "word_count"}.}

\item{file_col}{Character scalar or \code{NULL}. Column containing paths to text
files. If supplied with \code{measure = "file_size"}, file sizes are computed
with \code{\link[=file.info]{file.info()}}. If supplied with \code{measure = "word_count"}, words are
counted from the files.}

\item{text_col}{Character scalar or \code{NULL}. Column containing text strings to
measure directly.}

\item{measure}{Character scalar. Either \code{"file_size"} or \code{"word_count"}.}

\item{duration_unit}{Unit of \code{duration_col}: \code{"seconds"}, \code{"minutes"},
\code{"hours"}, or \code{"hms"} for strings like \code{"6:11:00"}.}

\item{output_unit}{Unit for the returned estimate column. Use \code{"hms"} for
spreadsheet-friendly strings like \code{"5:56:00"}.}

\item{method}{Estimation method. \code{"ratio"} fits a single seconds-per-unit
rate through the origin. \code{"lm"} fits a linear model with an intercept.}
}
\value{
A tibble containing \code{target} plus \code{.duration_seconds}, an
\verb{estimated_duration_*} column in \code{output_unit}, \code{.duration_measure}, and
\code{.duration_method}. The total estimated duration is also stored in the
\code{estimated_total_seconds} and \verb{estimated_total_*} attributes.
}
\description{
Estimate Spotify audiobook durations for new chapters or books from a
reference data set where Spotify duration is already known. The predictor can
be either text file size in bytes or word count. File size is often the
simplest option when all chapters are plain text files created by the same
workflow.
}
\examples{
reference <- tibble::tibble(
  book = c("A", "B"),
  file_size_bytes = c(100000, 150000),
  spotify_duration_minutes = c(120, 180)
)

chapters <- tibble::tibble(
  chapter = c("chapter_1", "chapter_2"),
  file_size_bytes = c(25000, 50000)
)

interpolate_spotify_audiobook_duration(
  reference,
  chapters,
  duration_col = "spotify_duration_minutes",
  size_col = "file_size_bytes",
  duration_unit = "minutes"
)
}
