% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_agreement.R
\name{model_rank_consistency}
\alias{model_rank_consistency}
\title{Compare model-derived rankings}
\usage{
model_rank_consistency(
  data,
  outcome = "mean_outcome",
  unit_by = c("book_id", "chapter_id"),
  rank_within = NULL,
  model_col = "model"
)
}
\arguments{
\item{data}{A data frame with one row per model-by-unit combination.}

\item{outcome}{Character string naming the score column (default
\code{"mean_outcome"}).}

\item{unit_by}{Character vector of columns that jointly identify a unit
(default \code{c("book_id", "chapter_id", "group")}).}

\item{rank_within}{Optional character vector of columns that define separate
ranking contexts (e.g., \code{"group"}). Items are ranked independently within
each combination of these columns.}

\item{model_col}{Character string naming the model column (default
\code{"model"}).}
}
\value{
A list with two elements:
\describe{
\item{ranks}{Tibble with the unit columns, \code{model}, \code{score}, and \code{rank}.}
\item{concordance}{Tibble with Kendall's W and associated statistics,
one row per \code{rank_within} combination (or one row total).}
}
}
\description{
Takes each model's continuous scores (1--100 scale) and derives rankings
from them, then evaluates cross-model concordance via Kendall's W. The
rankings are computed by the researcher from the raw scores — models
themselves only produce continuous ratings, not ordinal ranks. Useful for
answering "Do models rank books the same way?"
}
\details{
\code{unit_by} must identify exactly one row per model within each ranking context.
If the data still contain lower-level rows (for example, chapters) and you
want book-level ranks, aggregate those rows to the book level before calling
this function.

Units with missing scores for one or more models are excluded from the
concordance calculation. The reported \code{n_items} is the number of complete
items used.
}
\examples{
\dontrun{
rc <- model_rank_consistency(agg, outcome = "mean_rating",
  unit_by = c("book_id", "chapter_id"),
  rank_within = "group")
rc$concordance
rc$ranks

agg_book <- agg |>
  dplyr::group_by(model, book_id, group) |>
  dplyr::summarise(mean_rating = mean(mean_rating), .groups = "drop")

rc_book <- model_rank_consistency(agg_book, outcome = "mean_rating",
  unit_by = "book_id",
  rank_within = "group")
}
}
