% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_forest_books.R
\name{plot_forest_books}
\alias{plot_forest_books}
\title{Create a forest plot of book-level polarization reduction effects}
\usage{
plot_forest_books(
  forest_df,
  dv = "delta_gap",
  add_ci_label = TRUE,
  digits = 2,
  label_cols = c("book"),
  show_ci_label = TRUE,
  ci_multiline = TRUE,
  ci_show_party = FALSE,
  show_legend = TRUE,
  ci_label_fontsize = NULL,
  ci_label_lineheight = 0.85,
  header = NULL,
  title = "",
  xlab = "",
  zero = NA,
  show_overall = TRUE,
  ci.vertices = FALSE
)
}
\arguments{
\item{forest_df}{Either:
\itemize{
\item A data frame produced by \code{prepare_forest_books()} containing
\code{book}, \code{mean}, \code{lower}, and \code{upper}, or
\item A summary data frame (for example from
\code{summarize_chapter_scores(..., aggregate_level = "book")}) with
\verb{mean_\{dv\}} and \verb{sd_\{dv\}} columns.
}}

\item{dv}{Character. Variable prefix used when \code{forest_df} is not already
prepared. Defaults to \code{"delta_gap"}.}

\item{add_ci_label}{Logical. Passed to \code{prepare_forest_books()} when
internal preparation is needed. Defaults to \code{TRUE}.}

\item{digits}{Integer. Passed to \code{prepare_forest_books()} when internal
preparation is needed. Defaults to 2.}

\item{label_cols}{Character vector of left-side label columns.
Defaults to \code{"book"}.}

\item{show_ci_label}{Logical. If TRUE, appends an internally generated \code{ci}
label column to the right-side text.}

\item{ci_multiline}{Logical. For grouped party output, print each party CI on
its own line (using \verb{\\n}) when TRUE.}

\item{ci_show_party}{Logical. Include party names in CI labels.}

\item{show_legend}{Logical. Show party legend when grouped data are present.}

\item{ci_label_fontsize}{Optional numeric size for the CI label column.
Useful when grouped party CIs are shown on multiple lines.}

\item{ci_label_lineheight}{Numeric line height for the CI label column when
\code{ci_label_fontsize} is set.}

\item{header}{Labels of the columns to be displayed and as specified in
\code{label_cols}.}

\item{title}{Plot title}

\item{xlab}{X-axis label}

\item{zero}{Numeric scalar, NA, or NULL. Reference line position for
forestplot. Defaults to NA (no zero/reference line). NULL is
treated as NA for convenience.}

\item{show_overall}{Logical. If TRUE (default), a vertical dashed
line indicating the overall mean effect is added.}

\item{ci.vertices}{Logical. Whether to draw CI vertices in the forest plot.}
}
\value{
A \code{forestplot} grob object.
}
\description{
Generates a forest plot displaying mean reduction in affective
polarization across books, including 95\% confidence intervals.
}
\details{
Books are ordered from strongest to weakest mean effect.

The plot uses circular markers for point estimates and displays
95\% confidence intervals.

The vertical dashed line (if enabled) represents the average
effect across books.

The temporary \code{mean/lower/upper} columns required by \code{forestplot} are
generated internally when needed.

If \code{party} is present, estimates are drawn as multiple CIs per book row
(one row per book; one estimate per party).
}
\examples{
book_summary <- summarize_chapter_scores(
  toy_sim_results,
  aggregate_level = "book"
)
forest_df <- prepare_forest_books(book_summary, dv = "delta_gap")
plot_forest_books(
  forest_df,
  xlab = "Reduction in polarization gap",
  show_overall = FALSE
)

}
