% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_analysis.R
\name{run_text_analysis}
\alias{run_text_analysis}
\title{Run row-wise text analysis with a prompt template}
\usage{
run_text_analysis(
  data,
  text_col = "text",
  prompt,
  response_type,
  output_mode = c("structured", "text"),
  id_col = NULL,
  n_simulations = 1,
  temperature = 0,
  seed = 42,
  model = "gemini-2.5-flash-lite",
  integration = getOption("nalanda.integration"),
  virtual_key = getOption("nalanda.virtual_key"),
  base_url = getOption("nalanda.base_url"),
  excerpt_chars = 200,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{data}{A data frame with at least one text column.}

\item{text_col}{Name of the column containing the text to analyze.}

\item{prompt}{Character scalar prompt template. It may reference any columns
in \code{data} using \code{{column_name}} placeholders.}

\item{response_type}{An \code{ellmer} structured type specification, for example
\code{ellmer::type_object(score = ellmer::type_number())}.}

\item{output_mode}{Character. \code{"structured"} (default) uses the backend's
structured-output support. \code{"text"} is a compatibility mode for models
that do not support structured outputs (for example some Anthropic models):
nalanda appends strict JSON-only instructions to the prompt, calls the
model as free text, then parses the JSON back into the same fields.
Text mode is best-effort and stores the original model reply in
\code{raw_response}.}

\item{id_col}{Optional column name identifying each text row. When omitted, a
sequential \code{text_id} is created.}

\item{n_simulations}{Integer. Number of repeated runs per row.}

\item{temperature}{Numeric. Sampling temperature passed to the backend.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{model}{Character. Model name for the chat backend.}

\item{integration}{Optional Portkey/gateway route slug. Use a route returned
by \code{ellmer::models_portkey(base_url = "https://ai-gateway.apps.cloud.rt.nyu.edu/v1/")}
when working with the NYU gateway.}

\item{virtual_key}{Optional legacy virtual key.}

\item{base_url}{Character. Base URL for API calls.}

\item{excerpt_chars}{Integer. Number of text characters to retain in stored
prompt previews.}

\item{max_active}{Integer. Maximum number of concurrent requests passed to
\code{ellmer::parallel_chat_structured()} in structured mode. Text mode runs
plain chat requests sequentially.}

\item{rpm}{Integer. Requests-per-minute cap passed to
\code{ellmer::parallel_chat_structured()} in structured mode. Text mode runs
plain chat requests sequentially.}
}
\value{
A tibble containing the original row metadata, simulation index,
structured response fields, and stored prompt previews.
}
\description{
This function applies a prompt template to each row of a text dataset and
extracts structured responses with \code{ellmer}. It is designed for dataset-first
workflows such as sentiment, emotion, offensiveness, or moral-foundation
annotation across many short texts.
}
