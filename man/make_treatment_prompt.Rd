% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_treatment.R
\name{make_treatment_prompt}
\alias{make_treatment_prompt}
\alias{build_simulate_treatment_prompt}
\title{Build a concrete prompt for \code{simulate_treatment()}}
\usage{
make_treatment_prompt(
  prompt_template,
  intervention_text,
  identity_context = "",
  identity_label = NA_character_
)

build_simulate_treatment_prompt(
  prompt_template,
  intervention_text,
  identity_context = "",
  identity_label = NA_character_
)
}
\arguments{
\item{prompt_template}{Character scalar. A single prompt template that may
include \code{{intervention_text}}, \code{{identity}}, and \code{{group}}.}

\item{intervention_text}{Character scalar. The intervention text to insert
into \code{{intervention_text}}.}

\item{identity_context}{Character scalar. Optional identity context to
prepend to the prompt.}

\item{identity_label}{Character scalar. Optional identity label used to
expand \code{{identity}} and \code{{group}}.}
}
\value{
A character scalar containing the concrete prompt.
}
\description{
This helper expands a single \code{simulate_treatment()} prompt template into a
concrete prompt string. It is useful for inspecting prompt wording before
launching a run, much like \code{\link[=make_baseline_prompt]{make_baseline_prompt()}} and \code{\link[=make_post_prompt]{make_post_prompt()}}
are useful for \code{run_ai_on_chapters()}.
}
\examples{
make_treatment_prompt(
  prompt_template = "{intervention_text}\n\nRate this as {identity}.",
  intervention_text = "A short climate message.",
  identity_context = "You are simulating an American adult.",
  identity_label = "American"
)
}
