% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_simulation.R
\name{plan_survey_simulation}
\alias{plan_survey_simulation}
\alias{run_survey_simulation}
\title{Plan and run a multi-turn survey simulation}
\usage{
plan_survey_simulation(
  participants,
  survey_flow,
  participant_id = "participant_id",
  model_config,
  smoke_n = NULL
)

run_survey_simulation(
  participants,
  survey_flow,
  participant_id = "participant_id",
  model_config,
  memory = c("conversation", "profile", "block"),
  dry_run = FALSE,
  smoke_n = NULL,
  output_dir = NULL,
  resume = TRUE,
  on_error = c("stop", "continue"),
  integration = getOption("nalanda.integration"),
  virtual_key = getOption("nalanda.virtual_key"),
  base_url = getOption("nalanda.base_url"),
  excerpt_chars = 200,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{participants}{A data frame containing a unique \code{participant_id} column
(or the column named by \code{participant_id}).  Other columns are available as
\code{{placeholders}} in every screen prompt.}

\item{survey_flow}{A data frame with unique \code{screen_id} and \code{prompt} columns.
Optional columns are \code{block}, \code{response_type} (a list-column of ellmer
types), \code{output_mode}, and \code{display_if} (a logical scalar or a function of
\verb{(participant, answers)}).  \code{answers} is a named list of prior responses.}

\item{participant_id}{Stable participant identifier column.}

\item{model_config}{Model configuration accepted by \code{\link[=run_prompt_grid]{run_prompt_grid()}}.}

\item{smoke_n}{Restrict to the first participants for an inexpensive run.}

\item{memory}{One of \code{"conversation"} (all preceding turns remain in the
chat), \code{"profile"} (a fresh chat for each screen), or \code{"block"} (a fresh
chat at each block).  Profile/context placeholders are included in every
prompt under all policies.}

\item{dry_run}{Return a task plan without model calls.}

\item{output_dir}{Optional directory for atomic per-turn checkpoints.}

\item{resume}{Reuse compatible completed turn checkpoints.}

\item{on_error}{Either \code{"stop"} or \code{"continue"}.}

\item{...}{Global connection defaults: \code{integration}, \code{virtual_key},
\code{base_url}, \code{excerpt_chars}, \code{max_active}, and \code{rpm}.  The latter two are
recorded as provenance; turns for one respondent are deliberately serial.}
}
\value{
\code{plan_survey_simulation()} returns task rows.  The runner returns a
list with turn-level \code{results}, respondent-level \code{wide}, \code{plan}, \code{tasks},
and \code{errors}.
}
\description{
\code{run_survey_simulation()} simulates respondents moving through ordered survey
screens.  Unlike \code{\link[=run_prompt_grid]{run_prompt_grid()}}, screens are turns for the \emph{same}
respondent.  \code{participants} is always used row-wise: it is never crossed
with conditions or profile fields.
}
