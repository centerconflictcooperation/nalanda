% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_run_ai_metrics.R
\name{compute_run_ai_metrics}
\alias{compute_run_ai_metrics}
\title{Compute derived pre/post effect metrics from raw turn-level output}
\usage{
compute_run_ai_metrics(x, per_group = NULL)
}
\arguments{
\item{x}{A data frame or list-like object from \code{\link[=run_ai_on_chapters]{run_ai_on_chapters()}} with
turn-level rows including \code{chapter}, \code{sim}, \code{identity}, \code{turn_type}, and
\code{rating}. If a list is supplied, the function will attempt to combine its
data-frame elements with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}} before computing metrics. If
cumulative output from \code{\link[=run_ai_cumulative_chapters]{run_ai_cumulative_chapters()}} is detected, this
function delegates to \code{\link[=compute_run_ai_metrics_cumulative]{compute_run_ai_metrics_cumulative()}}.}

\item{per_group}{Optional logical. Whether the run used per-group mode
(\code{{group}} in question template). If \code{NULL} (default), mode is inferred
from \code{target_group}:
\itemize{
\item per-group if any non-missing \code{target_group} values exist;
\item single-question if \code{target_group} is entirely missing.
}}
}
\value{
A simulation-level tibble with derived metrics (for example
\code{pre_outgroup}, \code{post_outgroup}, \code{delta_outgroup}, and in per-group mode
also \code{pre_ingroup}, \code{post_ingroup}, \code{pre_gap}, \code{post_gap},
\code{delta_ingroup}, \code{delta_gap}). When present in the input, failed units
retain \code{error}, \code{error_turn}, and \code{error_message} diagnostics.
}
\description{
Separates post-processing from model execution so users can re-compute
metrics without re-running API calls.
}
\examples{
metrics <- compute_run_ai_metrics(toy_run_ai_turns)
head(metrics)

# The processed output can be passed on to summary and plotting helpers.
summary_by_chapter <- summarize_chapter_scores(metrics)
head(summary_by_chapter)
}
