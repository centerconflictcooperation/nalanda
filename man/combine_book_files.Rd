% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_book_files.R
\name{combine_book_files}
\alias{combine_book_files}
\title{Combine chapter text files into one numbered file per book}
\usage{
combine_book_files(
  input_dir,
  output_dir = file.path(input_dir, "combined"),
  separator = "\\n\\n",
  overwrite = FALSE
)
}
\arguments{
\item{input_dir}{Character scalar. Folder containing chapter \code{.txt} files.}

\item{output_dir}{Character scalar. Folder where combined \code{.txt} files should
be written. Defaults to a \verb{combined/} subfolder inside \code{input_dir}.}

\item{separator}{Character scalar. Text inserted between chapters when
combining them. Defaults to two line breaks.}

\item{overwrite}{Logical scalar. If \code{TRUE}, replace existing output files.
Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per combined book and columns describing the
numeric output file, original title stem, and source chapter numbers.
}
\description{
Takes chapter files named like \verb{1_howcanyou.txt} and \verb{2_howcanyou.txt},
groups them by the shared title stem after the underscore, orders chapters
by their numeric prefix, and writes one combined \code{.txt} file per group
using filenames like \verb{1_howcanyou.txt}.
}
