% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_agreement.R
\name{pairwise_for_level}
\alias{pairwise_for_level}
\title{Pairwise model correlations at a chosen analysis level}
\usage{
pairwise_for_level(
  data,
  outcome = "mean_outcome",
  unit_by = c("book_id", "chapter_id", "group"),
  group_by = NULL,
  model_col = "model",
  methods = c("pearson", "spearman"),
  drop_missing = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per model-by-unit combination.}

\item{outcome}{Character string naming the score column (default
\code{"mean_outcome"}).}

\item{unit_by}{Character vector of columns that jointly identify a unit
(default \code{c("book_id", "chapter_id", "group")}).}

\item{group_by}{Optional character vector. If provided, agreement metrics
are computed separately within each level of these columns (e.g.,
\code{"group"} to get separate estimates for Democrats and Republicans).}

\item{model_col}{Character string naming the model column (default
\code{"model"}).}

\item{methods}{Character vector of correlation types. One or both of
\code{"pearson"} and \code{"spearman"} (default both).}

\item{drop_missing}{Logical. Whether to drop rows with missing model, unit,
or grouping identifiers before aggregating (default \code{TRUE}).}
}
\value{
Output of \code{\link[=model_pairwise_cor]{model_pairwise_cor()}} for the requested level.
}
\description{
Aggregates lower-level rows to the requested unit level, then calls
\code{\link[=model_pairwise_cor]{model_pairwise_cor()}}. This is a convenience wrapper for cases where data
still contain chapter, party, or simulation-detail rows but the researcher
wants correlations at a broader level, such as book-level correlations.
}
\examples{
\dontrun{
# Book-level pairwise correlations from chapter-party-level aggregated data
pw_book <- pairwise_for_level(
  agg,
  outcome = "mean_delta_gap",
  unit_by = "book",
  model_col = "model",
  methods = "pearson"
)

summarize_model_correlations(pw_book, method = "pearson")
}
}
