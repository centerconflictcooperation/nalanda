% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_top_units.R
\name{plot_top_unit_pairs}
\alias{plot_top_unit_pairs}
\title{Plot paired subgroup ranks for top units}
\usage{
plot_top_unit_pairs(
  data,
  item_col = NULL,
  subgroup_col = "party",
  top_n_items = NULL,
  item_labels = NULL,
  subgroup_order = NULL,
  title = "Paired subgroup ranks",
  x_breaks = NULL,
  x_limits = NULL
)
}
\arguments{
\item{data}{Output of \code{\link[=summarize_top_units]{summarize_top_units()}} with a subgroup column such as
\code{"party"}.}

\item{item_col}{Character. Column identifying the ranked item. If \code{NULL},
the function tries to infer \code{"book"} or \code{"book_id"}.}

\item{subgroup_col}{Character. Two-level subgroup column to connect, e.g.
\code{"party"}.}

\item{top_n_items}{Optional integer. If supplied, keep only items with the
best average \code{mean_rank} across subgroups.}

\item{item_labels}{Optional character vector for display labels. Use a named
vector to map item IDs to labels, e.g. \code{c("1" = "Opening chapter")}. For
chapter-like IDs that start with numbers, an unnamed vector is matched by
chapter number.}

\item{subgroup_order}{Optional character vector giving the two subgroup
levels in display order.}

\item{title}{Optional plot title.}

\item{x_breaks}{Optional numeric vector of x-axis breaks. If \code{NULL}, integer
rank breaks are shown by default.}

\item{x_limits}{Optional numeric vector of length 2. If \code{NULL}, limits are
chosen from the displayed mean ranks.}
}
\value{
A ggplot2 object.
}
\description{
Creates a connected Cleveland dot plot from \code{\link[=summarize_top_units]{summarize_top_units()}} output
when rankings were computed within a two-level subgroup, such as party. Each
row is an item, dots show subgroup-specific mean ranks, and connecting lines
show how much the ranking differs between subgroups.
}
\examples{
\dontrun{
top_books_party <- summarize_top_units(
  agg,
  outcome = "mean_delta_gap",
  item_by = "book",
  rank_within = "party"
)
plot_top_unit_pairs(top_books_party, item_col = "book", subgroup_col = "party")
}
}
